# This file makes 'scrapers_v2' a Python package.

# Optionally, you can expose key components here for easier imports, e.g.:
# from .config import settings
# from .schema_adapter import UnifiedEvent, map_to_unified_schema
# from .utils import setup_logger, save_unified_events_to_mongodb

# For now, keeping it simple. Consumers can import directly from submodules.
# Example: from scrapers_v2.config import settings
# Example: from scrapers_v2.utils import setup_logger
# Example: from scrapers_v2.schema_adapter import UnifiedEvent
